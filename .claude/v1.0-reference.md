# RDLS v1.0 Reference

v1.0 is used for **all datasets being newly published or converted from v0.3**. This includes Tomorrow Cities, NISMOD ICRA, NISMOD SDK, MDG, Mombasa, and any future datasets. Do NOT break the existing v0.3 setup (pipeline still generates v0.3 for non-converted datasets).

## Schema and Validation

**Schema (authoritative)**: `C:\Users\benny\OneDrive\Documents\Github\rdl-standard\schema\rdls_schema.json`  
→ This is the regularly-synced official repo. Always use this, not the local snapshot.  
→ Local snapshot `schema/rdls_schema_v1.0.json` may be outdated.

**Template**: `schema/rdls_template_v1.0.json` — annotated with types, requirement levels, cross-field rules. No template exists in rdl-standard; keep consulting this local copy.

**Codelists**: `C:\Users\benny\OneDrive\Documents\Github\rdl-standard\schema\codelists\` (18 closed CSVs, 34 open CSVs)

**Validator**: `scripts/validate_records.py` (three-layer: JSON Schema → codelist CSV → semantic cross-field; logic in `src/audit.py`)
```bash
python scripts/validate_records.py <metadata.json>
```
Auto-detects: schema from rdl-standard (preferred) → local snapshot fallback; codelists from rdl-standard.

## v1.0 JSON Metadata Rules

**Structure**
- Every JSON MUST start with `{"datasets": [{"id": ...}]}` wrapper — never unwrapped
- If a field has no confirmed value, SKIP it — do not add placeholders or invented values
- Do not create fields outside the schema/template — fields are fixed
- Only use values sourced from: data review, report, data files, or codelists
- For `details` field: content from data review/report NOT already in `description`

**Entity fields** (publisher, contact_point, creator, attributions.entity)
- `name` always REQUIRED
- At least one of `email` or `url` REQUIRED (schema `anyOf`)
- Do not invent email addresses

**Source fields** (lineage.sources)
- Schema field is `name`, NOT `title` — v0.3 uses `title`, v1.0 uses `name`
- Optional enrichment: `type` (dataset/model), `risk_data_type`, `used_in`, `license`

## Key Differences from v0.3

| Feature | v0.3 | v1.0 |
|---------|------|------|
| `publisher/creator/contact_point` | Inside `attributions` array | Separate required top-level fields |
| `project` | Object `{name, url}` | Simple string |
| `sources` | Top-level `sources[]` | Inside `lineage.sources[]` |
| `license` | String codelist code | IRI string (URL) |
| `data_format` (resources) | Closed codelist | Replaced by `media_type` + `format` |
| `hazard_type` codelist | 11 types | +`convective_storm`, `dust_sand_storm`, `erosion`, `pest_infestation`, `sea_level_rise` |
| `process_type` codelist | No lightning | +`lightning`, `thunderstorm`, `hail`, `glacial_lake_outburst`, `coastal_erosion`, `soil_erosion`, `subsidence_uplift` |
| `occurrence` types | `probabilistic` only | +`empirical`, `deterministic` with `index_criteria` |
| `climate` on resources | Not available | `{model, scenario, percentile}` |
| `baseline_period` | Not available | Period object on resources |
| `spatial_aggregation` | Not available | Free text on resources |
| `loss.losses[].disaster_identifiers` | Not available | Optional array of Classification objects (GLIDE/EM-DAT event IDs) |

## Codelist Rules

**Closed** (must match exactly): `hazard_type`, `process_type`, `climate_scenario`, `analysis_type`, `frequency_distribution`, `risk_data_type`, `spatial_scale`, `data_calculation_type`, `source_type`, `function_approach`, `impact_type`, `loss_type`, `metric_dimension`, `relationship_type`, `exposure_category`, `seasonality`, `unit_currency`
→ Currently 18 closed CSVs at `rdl-standard/schema/codelists/closed/`

**Open** (standard preferred, custom allowed): `imt_*.csv`, `IMT.csv`, `unit_*.csv`, `roles.csv`, `license.csv`, `media_type.csv`, `location_gazetteers.csv`, `classification_scheme.csv`
→ Currently 34 open CSVs at `rdl-standard/schema/codelists/open/`

**IMT codes**: Format is `metric:unit` (e.g., `AirTemp:C`, `PGWS:m/s`, `pptn24:mm`, `HD:-`). Master `IMT.csv` has `Hazard` column linking codes to types, plus `universal` entries. Per-type files `imt_[type].csv` have type-specific codes. Validator checks both combined. **No `imt_sea_level_rise.csv`** — use IMT.csv universal entries for sea_level_rise.

**earthquake process inconsistency**: `process_type.csv` (closed codelist) has `rupture`. Schema conditional has `primary_rupture`/`secondary_rupture` — this is a schema bug. Use `rupture` (the CSV value) for earthquake events.

**Climate scenario**: Single string enum — cannot hold multiple scenarios. For multi-scenario resources: omit `scenario`, use `model` and `description`. Only set when resource covers exactly one scenario.

## Cross-Field Rules (8 rules)

1. **type → process**: Process must match hazard type (full mappings in template)
2. **type → IMT**: IMT codelist switches per type, all open
3. **quantity_kind → unit**: Unit codelist per quantity_kind. Currency is CLOSED (ISO 4217).
   Use `normalise_unit()` from `src/codelists.py` before writing any unit value.
   Common confirmed codes: `square_metre` (m2), `hectare` (ha), `metre` (m), `kilometre` (km)
4. **scale → countries**: global=none, regional=min 2, national/sub-national/urban=min 1
5. **analysis_type → occurrence**: probabilistic/empirical/deterministic must match. Do NOT mix within one event_set — split into separate event_sets
6. **Entity contact**: name + (email or url) required
7. **risk_data_type → sections**: hazard/exposure/vulnerability/loss sections match risk_data_type
8. **climate.scenario → baseline_period**: scenario present → baseline_period expected

## Event Set Design

- Split by analysis type: deterministic (means), probabilistic (return periods), empirical (observations) in separate event_sets
- `occurrence_range`: only for `analysis_type: "probabilistic"` — schema guidance
- `event_count`: only when events array is NOT populated
- Mean values: use deterministic event_set with `index_criteria`, or probabilistic with `return_period: 1`

## Common Pitfalls

| Issue | Fix |
|-------|-----|
| `sources[].title` instead of `name` | v1.0 uses `name`, not `title` |
| `climate.scenario` with multiple RCPs | Omit scenario, describe in `description` |
| Custom IMT like `deg_C` instead of `AirTemp:C` | Use standard codelist codes from IMT.csv |
| Mixed occurrence types in one event_set | Split into separate event_sets per analysis_type |
| `occurrence_range` on deterministic sets | Remove — only for probabilistic |
| Missing `{"datasets": [...]}` wrapper | Always wrap |

## Post-conversion checklist

**Run after EVERY v0.3 -> v1.0 conversion before considering it done.**

### Step 1 - Run the mechanical enrichment script

```bash
C:/Users/benny/miniforge3/envs/to-rdls/python.exe scripts/validate_records.py --enrich "output/<collection>/**/*.json"
```

This applies automatically:
- `unit=count` on every metric where `quantity_kind=count` and `unit` is absent
- GED4ALL URI fix: broken gemproducts URL (404) -> wiki URL
- `scheme="Custom"` removed (not a valid `classification_scheme.csv` code)
- `"Commercial"` and TODO strings removed from `lineage.sources[].license`
- `intensity_measure="wd:m"` on flood/coastal_flood losses with TODO IM
- `scheme="GED4ALL"` restored for known GED4ALL exposure item IDs

### Step 2 - Fix asset_type manually (per dataset)

For every exposure item the script flags as needing attention, set:
- `asset_type.id`: meaningful per-item identifier - e.g. `"buildings"`, `"population"`, `"roads"` - NEVER the scheme name (`"GED4ALL"`, `"Custom"`)
- `asset_type.title`: human-readable label (e.g. `"Building taxonomy"`, `"Population taxonomy"`)
- `asset_type.description`: data source + classification context sentence
- `asset_type.scheme`: `"GED4ALL"` if exposure fits any of the 10 GED4ALL categories; absent otherwise
- `asset_type.uri`: `https://wiki.openstreetmap.org/wiki/GED4ALL` for GED4ALL items; absent otherwise

Follow the pattern in `temp/enrich_wbgufra_asset_type.py` for dataset-specific enrichment scripts.

#### GED4ALL 10 categories (scheme="GED4ALL" applies)
1. Complete Buildings  2. Simplified Buildings  3. Road Network  4. Railway Network
5. Bridges  6. Pipelines and Storage Tanks  7. Power Grids  8. Energy Generation
9. Crops, Livestock and Forestry  10. Socio-Economic (population, GDP, economic indicators)

#### No scheme (absent) for
- `land_cover`, `exp_metrics_1` - general land cover not specifically in cat 9
- `exposure_mining` - no mining category in GED4ALL or any other open codelist scheme
- `exposure_landuse` - land use zoning, no matching scheme
- Any item using a **custom taxonomy** (Deltares FIAT typology, project-specific classification)

### Step 3 - Check media_type vs format

```bash
grep -r '"format"' output/<collection>/  # catch any stragglers
```

Rule: check `rdl-standard/schema/codelists/open/media_type.csv` first.
- If format has an IANA entry -> use `media_type` field, delete `format`
- If no entry -> use `format` (free text), no `media_type`
- NEVER use both together

Common mappings:
| Format | Field | Value |
|--------|-------|-------|
| File Geodatabase (gdb) | media_type | `application/x-filegdb` |
| GeoTIFF | media_type | `image/tiff;application=geotiff` |
| Shapefile | media_type | `application/x-shapefile` |
| GeoPackage | media_type | `application/geopackage+sqlite3` |
| GeoJSON | media_type | `application/geo+json` |
| CSV | media_type | `text/csv` |

### Step 4 - Backup/rename

```
<original>.json   ->  <original>_v03.json   (backup of v0.3 source)
<converted>.json  ->  <original>.json       (v1.0 takes original name, no _v10 suffix)
```

Exclude backups from all scripts: `if "_v03" not in f.name`

### Step 5 - Layer 1 validation

```bash
C:/Users/benny/miniforge3/envs/to-rdls/python.exe scripts/validate_records.py output/<collection>/<file>.json
```

Known false positives (do NOT fix our files):
- `links.href const mismatch`: **RESOLVED** in rdl-standard commit c7c969a - schema `$id` now correctly uses `1__0__0`. Semantic validator updated to accept both `0__3__0` and `1__0__0` as valid hrefs.
- `temporal oneOf false positives`: year-only dates (`1980`, `2080`) valid per pattern but trigger jsonschema when format_checker absent

### Step 6 - GED4ALL scheme (manual, dataset-specific)

If a new dataset uses GED4ALL taxonomy, add its exposure item IDs in the dataset-specific enrichment script (follow `temp/enrich_wbgufra_asset_type.py`). The `--enrich` mode in `validate_records.py` does NOT auto-set GED4ALL scheme - see note in `src/enrich.py`.

---

## Ad-hoc Metadata Creation - Enrichment Checklist

**For every ad-hoc metadata request: do NOT stop at required fields. Go through every section below and add every field where data supports it. The template is the authoritative source: `schema/rdls_template_v1.0.json`.**

### 0. Block ordering (always first)

Write fields in this exact order - the validator does not reorder, only validates:
```
id, title, description, risk_data_type,
publisher, version, purpose, project, details,
contact_point, creator,
spatial, spatial_resolution, temporal, temporal_resolution,
license, attributions, lineage, referenced_by, resources,
hazard, exposure, vulnerability, loss, links
```
Source of truth: `RDLS_V10_FIELD_ORDER` in `src/translate.py`.

---

### 1. Top-level fields

| Field | Req | Add when |
|-------|-----|----------|
| `id` | REQUIRED | Always - follow naming convention |
| `title` | REQUIRED | Always |
| `description` | REQUIRED | Always |
| `risk_data_type` | REQUIRED | Always - array |
| `publisher` (name + email/url) | REQUIRED | Always |
| `version` | optional | Version number is known (e.g. "1", "2.0") |
| `purpose` | optional | Intended use or reason for creation is stated |
| `project` (name + url) | optional | Dataset was produced under a named programme or project |
| `details` | optional | Methodology caveats, technical nuances, provenance notes NOT suitable for `description` |
| `contact_point` (name + email/url) | REQUIRED | Always - use first author if named, otherwise lead org |
| `creator` (name + email/url) | REQUIRED | Always - team or org that built the dataset |
| `spatial.scale` | optional | Always set - global/regional/national/sub-national/urban |
| `spatial.countries` | conditional | Required unless scale=global; min 2 for regional |
| `spatial.gazetteer_entries` | optional | Named place with a GeoNames/GADM entry |
| `spatial.bbox` | optional | Always include when known - [min_lon, min_lat, max_lon, max_lat] |
| `spatial.centroid` | optional | Point datasets or when bbox is not meaningful |
| `spatial_resolution` | optional | Raster or grid datasets - value in metres |
| `temporal.start` / `.end` | optional | Always include when coverage period is known |
| `temporal.duration` | optional | Duration known but exact dates are not (ISO 8601: P30Y, P6M) |
| `temporal.central_year` | optional | Single representative year (e.g. census year) |
| `temporal_resolution` | optional | Time step is known - ISO 8601 duration (P1D, P1M, P1Y) |
| `license` | REQUIRED | Always - IRI from license.csv or CC/OGL URL |
| `attributions[]` | optional | Collaborating institutions, funders, named PIs not captured as publisher/creator. Valid roles: `collaborator`, `funder`, `principal_investigator`, `co_author`, `processor`, `contributor`, `custodian`, `rights_holder` |
| `lineage.description` | optional | Always write a narrative of how the dataset was produced |
| `lineage.sources[]` | optional | List ACTUAL inputs: raw data → `type: dataset`; processing models/APIs → `type: model`. Never list the paper or hosting platform here. |
| `referenced_by[]` | optional | Every paper, report, or technical note that describes or cites the dataset. Include ALL `author_names`, `doi` (without https://doi.org/ prefix), `date_published` (YYYY-MM-DD) |
| `links` | REQUIRED | Always include describedby link to schema |

---

### 2. Resources (per resource item)

Create a separate entry for each distinct access method (download file, API, GEE, WMS, etc.).

| Field | Req | Add when |
|-------|-----|----------|
| `id`, `title`, `description` | REQUIRED | Always |
| `media_type` | optional | Format has an IANA entry in `media_type.csv` - check first |
| `format` | optional | No IANA entry - use free text. Never combine with `media_type` |
| `conforms_to` | optional | OGC API, STAC, or other standard - see `conforms_to.csv` |
| `spatial` | optional | Resource covers a different area than the dataset-level spatial |
| `spatial_resolution` | optional | Grid or raster resource - metres |
| `spatial_aggregation` | optional | Aggregation unit described in free text (e.g. "0.25-degree grid cells", "admin level 2") |
| `coordinate_system` | optional | Always add for geospatial files - EPSG:NNNN or ESRI:NNNN |
| `temporal.start` / `.end` | optional | Resource covers a specific sub-period of the dataset |
| `temporal_resolution` | optional | Time step for this specific file differs from dataset level |
| `download_url` | optional | Direct file download link - include whenever available |
| `access_url` | optional | Landing page or API endpoint - always include |
| `baseline_period` | conditional | Climate projection resource - reference period for the scenario |
| `climate.model` | optional | Climate model name (e.g. HadGEM3-GC31-LL, CORDEX ensemble) |
| `climate.scenario` | optional | Exactly ONE scenario (SSP/RCP code). Omit if resource covers multiple |
| `climate.percentile` | optional | Percentile of the ensemble (0-100) |

---

### 3. Hazard block

| Field | Req | Add when |
|-------|-----|----------|
| `event_sets[].id` | REQUIRED | Always |
| `event_sets[].hazards[].id` | REQUIRED | Always |
| `event_sets[].hazards[].type` | REQUIRED | Always - closed codelist |
| `event_sets[].hazards[].process` | REQUIRED | Always - must match type (rule 1). If dataset mixes process types, pick most representative and document in `classification.description` |
| `event_sets[].hazards[].intensity_measure` | REQUIRED | Always - even when dataset has no physical IM values; use conventional code and explain in `classification.description` |
| `event_sets[].hazards[].classification` | optional | Always useful: add taxonomy link (UNDRR-ISC, GLIDE) and use `description` to document mixed sub-types, IM caveats, or scope notes |
| `event_sets[].hazards[].trigger` | optional | Cascading hazards only (e.g. earthquake triggering tsunami) |
| `event_sets[].analysis_type` | REQUIRED | Always - empirical/deterministic/probabilistic |
| `event_sets[].calculation_method` | optional | Set when known: `observed` (field/news data), `simulated` (model), `inferred` (derived) |
| `event_sets[].frequency_distribution` | optional | Probabilistic datasets where distribution type is documented |
| `event_sets[].seasonality` | optional | When seasonal pattern is documented (uniform/user_defined) |
| `event_sets[].event_count` | optional | Total event count when `events[]` array is NOT populated |
| `event_sets[].occurrence_range` | optional | Probabilistic only - e.g. "10-10000 year return period" |
| `event_sets[].events[].id` | REQUIRED | Only when populating individual events |
| `event_sets[].events[].calculation_method` | REQUIRED | When populating events |
| `event_sets[].events[].occurrence` | REQUIRED | When populating events - match one sub-type to analysis_type |
| `event_sets[].events[].disaster_identifiers[]` | optional | When event maps to a known GLIDE/EM-DAT record |
| `event_sets[].events[].description` | optional | Event-specific notes |

---

### 4. Exposure block

| Field | Req | Add when |
|-------|-----|----------|
| `exposure[].id` | REQUIRED | Always |
| `exposure[].category` | REQUIRED | Always - closed codelist |
| `exposure[].asset_type` | optional | Always add: classification scheme (GED4ALL, HAZUS), human-readable title, description of asset taxonomy used, URI |
| `exposure[].metrics[].id` | REQUIRED | Always |
| `exposure[].metrics[].dimension` | REQUIRED | Always - structure/content/product/disruption/population/index |
| `exposure[].metrics[].measurement.quantity_kind` | optional | Add when known - area/count/currency/length/mass/etc. |
| `exposure[].metrics[].measurement.unit` | conditional | Required when quantity_kind is set (rule 3). Use `normalise_unit()` - never raw abbreviations |

---

### 5. Vulnerability block

| Field | Req | Add when |
|-------|-----|----------|
| `functions.vulnerability[]` / `fragility[]` / `damage_to_loss[]` / `engineering_demand[]` | optional | One per function type present in the dataset |
| `functions.*.id`, `approach`, `relationship` | REQUIRED per item | Always when function present |
| `functions.*.hazard_primary` | optional | Specify hazard type + IM the function applies to |
| `functions.*.hazard_secondary` | optional | Compound/cascading hazard functions |
| `functions.*.hazard_analysis_type` | optional | probabilistic/deterministic/empirical |
| `functions.*.category` | optional | Asset category the function applies to |
| `functions.*.impact` (type, modelling, metric, measurement) | optional | Add whenever impact type and metric are documented |
| `functions.*.taxonomy` | optional | Asset taxonomy used (GED4ALL, HAZUS, GEM) |
| `functions.*.analysis_details` | optional | Free text description of the function derivation |
| `functions.fragility[].damage_scale_name` | optional | HAZUS-MH, EMS-98, JMA, etc. |
| `functions.fragility[].damage_states_names[]` | optional | List of named damage states |
| `socio_economic[].id`, `indicator_name`, `indicator_code`, `description`, `reference_year` | REQUIRED per item | When vulnerability index / socio-economic indicator is included |
| `socio_economic[].scheme`, `threshold`, `uri`, `analysis_details` | optional | When source taxonomy, threshold definition, or external reference exists |

---

### 6. Loss block

| Field | Req | Add when |
|-------|-----|----------|
| `losses[].id` | REQUIRED | Always |
| `losses[].hazard.type` | REQUIRED | Always |
| `losses[].hazard.intensity_measure` | REQUIRED | Always (rule 2) |
| `losses[].hazard.process` | optional | Add when process type is known |
| `losses[].hazard.classification` | optional | Link to taxonomy; describe scope |
| `losses[].hazard.trigger` | optional | Cascading hazard only |
| `losses[].disaster_identifiers[]` | optional | When loss record maps to known GLIDE/EM-DAT event |
| `losses[].asset_category` | REQUIRED | Always - closed codelist |
| `losses[].asset_dimension` | REQUIRED | Always - closed codelist |
| `losses[].impact_and_losses.impact_type` | REQUIRED | direct/indirect/total |
| `losses[].impact_and_losses.impact_modelling` | REQUIRED | inferred/observed/simulated |
| `losses[].impact_and_losses.impact_metric` | REQUIRED | from impact_metric.csv |
| `losses[].impact_and_losses.measurement.quantity_kind` | optional | Add when known |
| `losses[].impact_and_losses.measurement.unit` | conditional | Required when quantity_kind set (rule 3) |
| `losses[].impact_and_losses.loss_type` | REQUIRED | ground_up/insured/gross/count/net_precat/net_postcat |
| `losses[].impact_and_losses.loss_approach` | REQUIRED | analytical/empirical/hybrid/judgement |
| `losses[].impact_and_losses.loss_frequency_type` | REQUIRED | probabilistic/deterministic/empirical |
| `losses[].description` | optional | Per-loss-entry notes |

---

### 7. Final steps

```bash
# Layer 1-3 validation
C:/Users/benny/miniforge3/envs/to-rdls/python.exe scripts/validate_records.py output/<collection>/metadata/<file>.json

# Mechanical enrichment check
C:/Users/benny/miniforge3/envs/to-rdls/python.exe scripts/validate_records.py --enrich "output/<collection>/metadata/*.json"
```

---

## Schema Changelog (rdl-standard syncs)

### 2026-05-01 - commit c7c969a

| Change | Impact on our records/code |
|--------|---------------------------|
| `rdls_schema.json` `$id` fixed: `0__2__0` -> `1__0__0` | `links.href` validator warning resolved - our v1.0 files were correct all along |
| New field: `loss.losses[].disaster_identifiers` (optional array of Classification) | Added to `rdls_template_v1.0.json`; use for DesInventar/EM-DAT linked loss records |
| `unit_currency.csv`: `SLL` (Leone) replaced by `SLE`; `ANG`, `BGN`, `CUC`, `HRK` moved to historical; added `VED`, `XAD`, `XCG`, `ZWG` | `src/codelists.py` loads from CSV directly - picks up automatically |
| `imt.csv`: `slr:cm` hazard affiliation changed from `flood,tsunami,coastal_flood` to `sea_level_rise` | Open codelist - no code change; `sea_level_rise` is now the correct hazard type for `slr:cm` |
| `license.csv`: Added `CC BY-NC 4.0` (`https://creativecommons.org/licenses/by-nc/4.0/`) | Already in `LICENSE_URL_MAP` in converter; semantic validator will now pass |
| `classification_scheme.csv`: `GLIDE` typo fixed (`Distaster` -> `Disaster`) | No code impact |
